یک مثال عالی برای این بخش، تابعی است که نام ماه را برمی‌گرداند:

```c
char *month_name(int n) {
    static char *name[] = {
        "Illegal month",
        "January", "February", "March",
        // ...
    };
    return (n < 1 || n > 12) ? name[0] : name[n];
}
```