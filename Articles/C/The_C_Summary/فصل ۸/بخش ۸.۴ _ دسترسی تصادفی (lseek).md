به طور معمول، خواندن و نوشتن به صورت ترتیبی انجام می‌شود. اما تابع `lseek` به شما اجازه می‌دهد بدون خواندن داده‌های میانی، مستقیماً به نقطه خاصی از فایل بپرید.

```c
long lseek(int fd, long offset, int origin);
```

- **`origin`**: می‌تواند شروع فایل (0)، موقعیت فعلی (1) یا انتهای فایل (2) باشد.