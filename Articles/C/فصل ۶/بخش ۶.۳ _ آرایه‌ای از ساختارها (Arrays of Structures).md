فرض کنید می‌خواهید کلمات کلیدی زبان C را بشمارید. بهترین راه، داشتن آرایه‌ای است که هر خانه‌اش شامل "کلمه" و "تعداد تکرار" باشد.

```c
struct key {
    char *word;
    int count;
} keytab[NKEYS];
```

برای دسترسی به کلمه سومین خانه آرایه: `keytab[2].word`